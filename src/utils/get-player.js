/**
 * Utility function to get the player object from the DOM.
 *
 * @returns {HTMLDivElement} - The player element.
 */
export function getPlayer() {
  return document.getElementsByClassName('inline_player')[0];
}

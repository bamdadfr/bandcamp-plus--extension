/**
 * Utility function to get the audio element from the given element.
 *
 * @returns {HTMLAudioElement} - The audio element.
 */
export function getAudio() {
  return document.getElementsByTagName('audio')[0];
}

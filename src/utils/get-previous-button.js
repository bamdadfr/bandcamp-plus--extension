/**
 * Utility function to get the previous button in the DOM.
 *
 * @returns {HTMLDivElement} - The previous button.
 */
export function getPreviousButton() {
  return document.getElementsByClassName('prevbutton')[0];
}

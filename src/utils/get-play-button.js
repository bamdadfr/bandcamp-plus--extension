/**
 * Utility function to get the play button element.
 *
 * @returns {HTMLSpanElement} - The play button element.
 */
export function getPlayButton() {
  return document.getElementsByClassName('playbutton')[0];
}
